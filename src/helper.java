import java.io.File;
import java.util.ArrayList;

public class helper {
    // i did this arraylist in the helper class because i want to use more than one.
    // and also i created a text file with name of votes.txt

    ArrayList<Integer> numberCandidate = new ArrayList<Integer>();
    ArrayList<String> CandidateName = new ArrayList<String>();
    File file = new File("votes.txt");

}
